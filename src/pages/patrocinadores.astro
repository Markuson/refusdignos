---
// Page: Patrocinadores (/patrocinadores)
// Description: Showcase sponsors and encourage new sponsorships

import BaseLayout from '../layouts/BaseLayout.astro';
import { colaboradores } from '../data/colaboradores';
---

<BaseLayout
  title="Nuestros Patrocinadores - Refugios Libres Dignos"
  description="Conoce a quienes hacen posible que sigamos devolviendo dignidad a los refugios libres. Empresas, instituciones y particulares comprometidos con la montaña."
>
  <!-- Hero Section -->
  <section class="bg-background-dark text-forest-green" style="padding-top: 120px; padding-bottom: 80px;">
    <div class="container mx-auto px-4 text-center">
      <div class="max-w-3xl mx-auto">
        <h1 class="text-4xl md:text-5xl font-bold mb-4 fade-in-up">Nuestros Patrocinadores</h1>
        <p class="text-xl md:text-2xl fade-in-up">
          Gracias a quienes hacen posible este proyecto
        </p>
      </div>
    </div>
  </section>

  <!-- Intro Section -->
  <section class="py-16 bg-background-light">
    <div class="container mx-auto px-4">
      <div class="max-w-3xl mx-auto text-center">
        <p class="text-lg text-forest-green/90 mb-8">
          Refugios Libres Dignos no sería posible sin el apoyo de empresas, instituciones y
          particulares que creen en nuestro mensaje: lo que es de todos, todos debemos cuidarlo.
          Cada contribución, grande o pequeña, nos ayuda a seguir devolviendo dignidad a los
          refugios libres.
        </p>
      </div>
    </div>
  </section>

  <!-- Empresas Section -->
  <section class="py-16 bg-background-dark">
    <div class="container mx-auto px-4">
      <h2 class="text-3xl font-bold text-forest-green mb-12 text-center">Nuestros Colaboradores</h2>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 max-w-6xl mx-auto">
        {
          colaboradores.map((colaborador) => (
            <a
              href={colaborador.url}
              target="_blank"
              rel="noopener noreferrer"
              class="bg-white p-6 rounded-lg shadow-md hover:shadow-xl transition-all duration-300 hover:scale-105 block"
            >
              <div class="text-center mb-4">
                {colaborador.logo ? (
                  <div class="w-full h-24 mx-auto mb-3 flex items-center justify-center">
                    <img
                      src={colaborador.logo}
                      alt={`Logo de ${colaborador.nombre}`}
                      class="max-w-full max-h-full object-contain"
                      style={colaborador.nombre === 'Julián Mairal' ? 'filter: brightness(0) saturate(100%);' : ''}
                    />
                  </div>
                ) : (
                  <div class="w-20 h-20 mx-auto mb-3 bg-tan/30 rounded flex items-center justify-center">
                    <span class="text-forest-green/50 text-xs">Logo</span>
                  </div>
                )}
                <h3 class="text-base text-forest-green font-semibold mb-3">{colaborador.nombre}</h3>
              </div>
              <p class="text-xs text-forest-green/70 text-center">{colaborador.descripcion}</p>
            </a>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Support Message Section -->
  <section class="py-16 bg-background-light">
    <div class="container mx-auto px-4">
      <div class="max-w-3xl mx-auto text-center">
        <h2 class="text-3xl font-bold text-forest-green mb-6">Gracias por hacer esto posible</h2>
        <p class="text-lg text-forest-green/90 mb-6">
          Cada uno de estos colaboradores aporta su granito de arena para que los refugios libres
          puedan seguir siendo espacios dignos y acogedores. Desde productos de calidad
          hasta servicios especializados, todos son parte fundamental de este proyecto.
        </p>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-16 bg-tan/30">
    <div class="container mx-auto px-4">
      <div class="max-w-3xl mx-auto text-center">
        <h2 class="text-3xl md:text-4xl font-bold text-forest-green mb-6">¿Quieres sumarte?</h2>
        <p class="text-lg text-forest-green/90 mb-8">
          Hay muchas formas de colaborar: patrocinio económico, donación de materiales,
          apadrinando un refugio, o simplemente difundiendo el proyecto.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a href="/contacto" class="btn btn-primary">
            Contáctanos
          </a>
          <a href="/proyecto" class="btn btn-primary">
            Conoce el Proyecto
          </a>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>
