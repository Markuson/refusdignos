---
// Page: Refugios Listing (/refugios)
// Description: Display all refuge rehabilitation projects

import BaseLayout from '../../layouts/BaseLayout.astro';

// Placeholder data for refuges (will be replaced with Content Collections in Week 3)
const refugios = [
  {
    id: 1,
    nombre: 'Refugio de Montgarri',
    ubicacion: 'Valle de Arán, Lleida',
    estado: 'En obra',
    descripcion: 'Antiguo refugio del siglo XIX en proceso de rehabilitación para acoger a víctimas de violencia de género.',
    imagen: '/favicon.svg', // Placeholder
  },
  {
    id: 2,
    nombre: 'Refugio de Pinet',
    ubicacion: 'Pallars Sobirà, Lleida',
    estado: 'Planificado',
    descripcion: 'Refugio histórico que será dedicado a jóvenes en riesgo de exclusión social.',
    imagen: '/favicon.svg', // Placeholder
  },
  {
    id: 3,
    nombre: 'Refugio de Salau',
    ubicacion: 'Alto Pallars, Lleida',
    estado: 'Finalizado',
    descripcion: 'Primer refugio completado, brindado a personas en situación de sinhogarismo.',
    imagen: '/favicon.svg', // Placeholder
  },
];

function getEstadoBadgeColor(estado: string) {
  switch (estado.toLowerCase()) {
    case 'finalizado':
      return 'bg-lime-green';
    case 'en obra':
      return 'bg-sunrise-orange';
    case 'planificado':
      return 'bg-off-white text-forest-green';
    default:
      return 'bg-off-white text-forest-green';
  }
}
---

<BaseLayout
  title="Nuestros Refugios - RefugiosLibresDignos"
  description="Descubre los refugios de montaña que estamos rehabilitando para crear espacios seguros, libres y dignos."
>
  <!-- Hero Section -->
  <section class="bg-forest-green text-white pb-16 md:pb-24" style="margin-top: -64px; padding-top: 80px;">
    <div class="container mx-auto px-4 text-center">
      <h1 class="text-4xl md:text-5xl font-bold mb-6">Nuestros Refugios</h1>
      <p class="text-xl md:text-2xl max-w-3xl mx-auto">
        Proyectos de rehabilitación de refugios libres de montaña
      </p>
    </div>
  </section>

  <!-- Refuges Grid -->
  <section class="py-16 bg-white">
    <div class="container mx-auto px-4">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {
          refugios.map((refugio) => (
            <article class="bg-white rounded-lg shadow-md overflow-hidden hover:shadow-xl transition-shadow duration-300">
              {/* Placeholder Image */}
              <div class="bg-tan/30 h-48 flex items-center justify-center">
                <span class="text-forest-green/50 text-sm">Imagen próximamente</span>
              </div>

              {/* Card Content */}
              <div class="p-6">
                <div class="flex items-start justify-between mb-3">
                  <h2 class="text-2xl font-semibold text-forest-green">{refugio.nombre}</h2>
                  <span
                    class={`px-3 py-1 rounded-full text-xs font-semibold text-white ${getEstadoBadgeColor(refugio.estado)}`}
                  >
                    {refugio.estado}
                  </span>
                </div>

                <p class="text-sm text-lime-green mb-3">📍 {refugio.ubicacion}</p>

                <p class="text-forest-green/80 mb-4 line-clamp-3">{refugio.descripcion}</p>

                <a
                  href={`/refugios/${refugio.id}`}
                  class="text-sunrise-orange hover:text-sunrise-orange/80 font-semibold transition-colors"
                >
                  Leer más →
                </a>
              </div>
            </article>
          ))
        }
      </div>

      {/* Empty State (if no refugios) */}
      {
        refugios.length === 0 && (
          <div class="text-center py-16">
            <p class="text-xl text-forest-green/70">
              Pronto tendremos refugios aquí. ¡Estamos trabajando en ello!
            </p>
          </div>
        )
      }
    </div>
  </section>

  <!-- Info Section -->
  <section class="py-16 bg-tan/30">
    <div class="container mx-auto px-4">
      <div class="max-w-3xl mx-auto text-center">
        <h2 class="text-3xl font-bold text-forest-green mb-6">
          Cada Refugio, Una Historia
        </h2>
        <p class="text-lg text-forest-green/90 mb-8">
          Cada refugio que rehabilitamos es "brindado" a un colectivo específico. No son
          simplemente espacios vacíos, sino hogares temporales llenos de dignidad y esperanza.
        </p>
        <a href="/proyecto" class="btn-secondary">
          Conoce Nuestro Proyecto
        </a>
      </div>
    </div>
  </section>
</BaseLayout>
